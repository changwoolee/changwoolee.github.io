Numbers of neural networks have been trained by either the cross entropy loss or the mean squared error (MSE). But why should we use them? And where are they from?

Before we go deeper, let's recap these losses.  
